public enum MentalState {
    Sane,
    Anxious,
    Insane,
}

public enum Location {
    Map,
    House,
    Work,
    Club,
    Hospital,
}

public enum DayOfWeek {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
}

public enum TimeOfDay {
    Morning,
    Noon,
    Evening,
    Night,
}