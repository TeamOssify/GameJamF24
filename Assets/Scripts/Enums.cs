public enum MentalState {
    Sane,
    Anxious,
    Insane,
}

public enum Location {
    Map,
    House,
    Work,
    Club,
    Hospital,
}

public enum TimeOfDay {
    Morning,
    Noon,
    Evening,
    Night,
}